/**
 * 급여 시스템 설정 파일
 * 
 * 이 파일은 급여 계산 및 처리에 관련된 모든 중요 설정값을 포함합니다.
 * 시스템 관리자나 개발자가 필요에 따라 설정을 변경할 수 있습니다.
 */

// ========== 급여 지급일 설정 ==========
/**
 * 기본 급여 지급일
 * 매월 해당 일자에 급여가 지급됩니다.
 * 유효한 값: 1-31 사이의 정수
 * 
 * 참고:
 * - 지급일이 1일인 경우: 전월 전체 기간이 정산 대상
 * - 지급일이 2일 이상인 경우: 전월 지급일부터 당월 지급일-1일까지가 정산 대상
 * - 지급일이 해당 월의 일수보다 클 경우 자동으로 마지막 날로 조정됨
 */
export const DEFAULT_PAYMENT_DAY = 1; // 매월 10일을 기본 지급일로 설정

/**
 * 급여 지급일 선택 옵션
 * UI에서 사용자가 선택할 수 있는 지급일 목록
 * UI를 통한 변경이 필요하지 않을 경우 빈 배열로 설정하면 선택 UI가 표시되지 않음
 */
export const PAYMENT_DAY_OPTIONS = []; // [] 빈 배열로 설정하면 UI에서 지급일 선택 옵션이 표시되지 않음

// ========== 급여 계산 설정 ==========
/**
 * 급여일 다음날이 공휴일인 경우 자동 조정 여부
 * true로 설정 시 급여일 다음날이 공휴일이면 급여일을 앞당겨 처리
 */
export const ADJUST_FOR_HOLIDAYS = false;

/**
 * 기타 급여 설정
 */
export const PAYROLL_SETTINGS = {
  DEFAULT_DEDUCTION_RATE: 0.15, // 기본 공제율
  OVERTIME_MULTIPLIER: 1.5,    // 연장근로 할증률
  NIGHT_MULTIPLIER: 1.5,       // 야간근로 할증률
  HOLIDAY_MULTIPLIER: 1.5      // 휴일근로 할증률
}; 